# URL base de la API
# Si no se define, se usan rutas relativas (funciona con el proxy de Vite en desarrollo)
# En producción, definir con la URL completa de la API
# VITE_API_URL=https://cira-web-api.onrender.com
