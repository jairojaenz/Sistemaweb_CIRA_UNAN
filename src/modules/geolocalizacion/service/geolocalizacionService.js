/**
 * Geolocalización — capa de datos para el mapa global.
 * Se conectará a clientes y muestras con coordenadas en la siguiente fase.
 */

/** @returns {Promise<Array<object>>} */
export async function getClientesGeolocalizacion() {
  return [];
}

/** @returns {Promise<Array<object>>} */
export async function getMuestrasGeolocalizacion() {
  return [];
}
